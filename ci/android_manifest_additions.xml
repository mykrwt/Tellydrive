<!--
  This is a REFERENCE manifest fragment, not a full Android project.

  TellyBase's lib/ source tree is a standard Flutter package; the
  android/ and ios/ platform folders themselves are normally generated by
  running `flutter create .` at the repo root (they're mostly boilerplate
  that the Flutter tool keeps in sync with your installed SDK version).

  After running `flutter create .`, merge the permissions and application
  attributes below into the generated android/app/src/main/AndroidManifest.xml.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Raw TCP socket to Telegram's MTProto data centers (port 443). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Smart Backup: reading Camera/Downloads/Documents folders. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Background uploads/downloads via WorkManager. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Upload/download progress notifications. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="TellyBase"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false">
        <!-- Standard <activity>/<meta-data> entries generated by
             `flutter create .` go here unmodified. -->
    </application>
</manifest>
